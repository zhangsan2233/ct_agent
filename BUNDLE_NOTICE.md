# Sanitized Copy Notice

This local copy excludes `.env` and the machine-specific virtual environment. A pinned
`requirements-lock.txt` is provided so the main environment can be recreated.

This copy contains CT-RATE volumes, reports, labels, metadata, case indexes, and preview
images. CT-RATE prohibits redistribution of dataset content. Remove those assets before
sharing the project; each recipient must obtain access with their own Hugging Face account.

Included assets:

- ChestCT-Agent source code and tests
- Official CT-CLIP source code
- `CT-CLIP_v2.pt` model checkpoint
- Text-classifier model and aggregate evaluation metrics
- CT-RATE data and generated previews for local use only
- A pinned `requirements-lock.txt` for recreating the main Python environment

Create a local `.env` from `.env.example`. Do not commit or share API tokens.
